# Twenty CRM Docker Configuration
# Copy to .env and adjust as needed

# Docker image tag
TAG=latest

# Port mapping — 3020 avoids conflict with Harbor Space (3000)
TWENTY_PORT=3020

# Server URL (must match the port above)
SERVER_URL=http://localhost:3020

# Database password (no special characters)
PG_DATABASE_PASSWORD=twenty_dev_password

# App secret — generate with: openssl rand -base64 32
APP_SECRET=change-me-to-a-random-secret-string-32chars

# Pre-fill sign-in for demo (set to false in production)
SIGN_IN_PREFILLED=true
